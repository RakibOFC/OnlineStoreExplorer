<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OnlineStoreExplorer" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.OnlineStoreExplorer" parent="Base.Theme.OnlineStoreExplorer" />
</resources>